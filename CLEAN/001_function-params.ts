function createMenu(
    title: string,
    body: string,
    buttonText: string,
    cancellable: boolean,
    buttonColor: string,
    cancelButtonColor: string
  ) {
    // <NO CODE NEEDED>
  }
  
  createMenu("Foo", "Bar", "Baz", true, null, null);
