/**
 * GOAL: refactor the code below to make use of ES6 capabilities
 * to make the code simpler and easier to read. Add any of your thought
 * processes as comments
 */

const colors = ['#123', '#456', '#678'];


const red = colors[0];
const blue = colors[1];
const green = colors[3];

console.log(`${red}, ${blue}, ${green}`);
